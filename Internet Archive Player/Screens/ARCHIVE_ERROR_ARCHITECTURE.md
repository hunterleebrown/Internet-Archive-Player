# Archive Error System Architecture

## Component Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         Home View (Root)                        │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │   .archiveErrorOverlay($errorManager.errorMessage)        │ │
│  │                                                             │ │
│  │   Observes: ArchiveErrorManager.shared.errorMessage       │ │
│  │   Displays: ArchiveErrorOverlay when error exists         │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │  SearchView  │  │ ListsView    │  │  Detail      │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ Uses
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│              ArchiveErrorManager (Singleton)                    │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  @Published var errorMessage: String?                     │ │
│  │                                                             │ │
│  │  func showError(_ error: ArchiveServiceError)             │ │
│  │  func showError(_ error: Error)                           │ │
│  │  func showError(message: String)                          │ │
│  │  func clearError()                                        │ │
│  └───────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                              ▲
                              │ Called by
                              │
┌─────────────────────────────────────────────────────────────────┐
│                      ViewModels Layer                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │ DetailVM     │  │ SearchVM     │  │  CacheVM     │         │
│  │              │  │              │  │              │         │
│  │ do {         │  │ do {         │  │ do {         │         │
│  │   try await  │  │   try await  │  │   try await  │         │
│  │ } catch {    │  │ } catch {    │  │ } catch {    │         │
│  │   Manager    │  │   Manager    │  │   Manager    │         │
│  │   .showError │  │   .showError │  │   .showError │         │
│  │ }            │  │ }            │  │ }            │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ Calls
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                   PlayerArchiveService                          │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  func getArchiveAsync(with:) throws -> Archive            │ │
│  │  func searchPPSAsync(...) throws -> SearchResponse        │ │
│  │  func getCollections(from:) throws -> CollectionResponse  │ │
│  └───────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ Can throw
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    ArchiveServiceError                          │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  • badIdentifier                                          │ │
│  │  • unexpectedHttpResponseCode                             │ │
│  │  • unknown                                                │ │
│  │  • nodata                                                 │ │
│  │  • emptyQueryString                                       │ │
│  │  • badParameters                                          │ │
│  │  • decodingError(errorMessage: String)                   │ │
│  └───────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

## Flow Diagram: Error Display

```
┌──────────────┐
│ User Action  │
│ (Search,     │
│  Load Item)  │
└──────┬───────┘
       │
       ▼
┌──────────────────────┐
│ ViewModel calls      │
│ PlayerArchiveService │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐        ┌──────────────────────┐
│ Service Call         │───────>│ Success ✓            │
│ (async/throws)       │        │ Data returned        │
└──────┬───────────────┘        └──────────────────────┘
       │
       │ throws error
       ▼
┌──────────────────────┐
│ catch block          │
│ ArchiveServiceError  │
└──────┬───────────────┘
       │
       ├─────────────────────────────────────┐
       │                                     │
       ▼                                     ▼
┌──────────────────────┐           ┌────────────────────┐
│ Local Error          │           │ Universal Error    │
│ Handling             │           │ Handling           │
│                      │           │                    │
│ self.errorMessage =  │           │ ArchiveErrorMgr    │
│ self.hasError = true │           │ .shared.showError()│
└──────────────────────┘           └────────┬───────────┘
                                            │
                                            ▼
                                   ┌────────────────────┐
                                   │ Manager publishes  │
                                   │ error message      │
                                   └────────┬───────────┘
                                            │
                                            ▼
                                   ┌────────────────────┐
                                   │ Home view observes │
                                   │ change in @State   │
                                   └────────┬───────────┘
                                            │
                                            ▼
                                   ┌────────────────────┐
                                   │ ArchiveErrorOverlay│
                                   │ appears with       │
                                   │ animation          │
                                   └────────┬───────────┘
                                            │
                                            ▼
                                   ┌────────────────────┐
                                   │ User sees error    │
                                   │ • Reads message    │
                                   │ • Taps dismiss     │
                                   └────────┬───────────┘
                                            │
                                            ▼
                                   ┌────────────────────┐
                                   │ errorMessage = nil │
                                   │ Overlay dismisses  │
                                   └────────────────────┘
```

## Integration Points

### 1. Home.swift
```swift
struct Home: View {
    @StateObject var errorManager = ArchiveErrorManager.shared
    
    var body: some View {
        ZStack {
            // ... existing content ...
        }
        .archiveErrorOverlay($errorManager.errorMessage)
    }
}
```

### 2. DetailViewModel.swift
```swift
catch let error as ArchiveServiceError {
    self.errorMessage = "Failed: \(error.description)"
    self.hasError = true
    
    // 🎯 Universal overlay
    ArchiveErrorManager.shared.showError(error)
}
```

### 3. SearchView.swift
```swift
catch let error as ArchiveServiceError {
    self.archiveError = error.description
    
    // 🎯 Universal overlay
    Task { @MainActor in
        ArchiveErrorManager.shared.showError(error)
    }
}
```

### 4. CollectionFilterCache.swift
```swift
catch let error as ArchiveServiceError {
    errorMessage = "Failed to load: \(error.description)"
    
    // 🎯 Universal overlay
    ArchiveErrorManager.shared.showError(error)
}
```

## Key Features

### ✨ Dual Error Handling
- **Local**: ViewModels track errors for local UI (e.g., inline error messages)
- **Global**: ArchiveErrorManager shows overlay for critical errors

### 🎭 User Dismissal Options
1. X button in header
2. "Dismiss" button at bottom
3. Tap backdrop outside card

### 🎨 Consistent Styling
- Uses app's `.fairyRed` accent color
- Matches app's design language
- Smooth spring animations

### 🔄 State Management
- Single source of truth (`ArchiveErrorManager.shared`)
- Observable via `@Published` property
- Thread-safe with `@MainActor`
